[pytest]
asyncio_mode = auto
markers =
    smoke: fast, cost-free checks safe for PR CI
    eval: slower evaluation (may need live LLM)
    needs_db: requires a reachable Postgres DATABASE_URL
filterwarnings =
    ignore::DeprecationWarning
